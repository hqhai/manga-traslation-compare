# JS/TS Backend Patterns

- Controller → Service → Repository layering
- DTO validation (zod/yup/class-validator)
- Centralized error middleware
- Request logging (pino/winston)
- Config via env + schema
